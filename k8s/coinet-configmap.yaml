apiVersion: v1
kind: ConfigMap
metadata:
  name: coinet-config
  labels:
    app: coinet-frontend
    environment: production
    managed-by: helm
    chart: coinet-frontend
    release: coinet
    heritage: Helm
    version: v1
    description: "Frontend environment config for Coinet"
data:
  API_URL: "https://api.coinet.com" 