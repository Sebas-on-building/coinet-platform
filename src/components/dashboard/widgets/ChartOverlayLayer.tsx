import React from "react";

/**
 * ChartOverlayLayer - Intentional no-op
 *
 * Reserved for future chart overlays (AI annotations, news markers, etc.).
 * Returns null until overlay features are implemented.
 * Used as a placeholder in MinimalistCanvasWidget for consistent layout.
 */
export const ChartOverlayLayer: React.FC = () => {
  return null;
}; 