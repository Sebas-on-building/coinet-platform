import React from "react";

/**
 * ChartInteractionLayer - Intentional no-op
 *
 * Reserved for future chart interaction features (zoom, pan, crosshair, etc.).
 * Returns null until interaction layer is implemented.
 * Used as a placeholder in MinimalistCanvasWidget for consistent layout.
 */
export const ChartInteractionLayer: React.FC = () => {
  return null;
}; 