apiVersion: v1
kind: ConfigMap
metadata:
  name: security-rbac
  labels:
    app: coinet-platform
    feature: security
    subfeature: rbac
data:
  description: "Atomic RBAC configuration for Coinet Platform." 