import { /*Event*/ } from 'shared-models/user-experience/events';

export function onAnalyticsEvent(event) {
  // Update projections, trigger downstream
  // Extensible: add listeners for analytics, reporting
}
