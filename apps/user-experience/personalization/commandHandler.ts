import { /*Command*/ } from 'shared-models/user-experience/commands';

export function handlePersonalizationCommand(cmd) {
  // Validate, persist, emit event
  // Extensible: add hooks for new types
}
