import { /*Event*/ } from 'shared-models/client-mobile/events';

export function onClientMobileEvent(event: unknown) {
  // Update projections, trigger downstream
  // Extensible: add listeners for analytics, reporting
}
