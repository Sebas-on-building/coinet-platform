import { /*Command*/ } from 'shared-models/client-mobile/commands';

export function handleClientMobileCommand(cmd: unknown) {
  // Validate, persist, emit event
  // Extensible: add hooks for new types
}
