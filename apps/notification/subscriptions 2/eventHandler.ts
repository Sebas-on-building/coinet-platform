import { /*Event*/ } from 'shared-models/notification/events';

export function onSubscriptionsEvent(event) {
  // Update projections, trigger downstream
  // Extensible: add listeners for analytics, reporting
}
