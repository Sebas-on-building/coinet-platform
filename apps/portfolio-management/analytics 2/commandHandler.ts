import { /*Command*/ } from 'shared-models/portfolio-management/commands';

export function handleAnalyticsCommand(cmd) {
  // Validate, persist, emit event
  // Extensible: add hooks for new types
}
