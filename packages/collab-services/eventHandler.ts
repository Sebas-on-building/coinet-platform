import { /*Event*/ } from 'shared-models/collab-services/events';

export function onCollabServicesEvent(event: unknown) {
  // Update projections, trigger downstream
  // Extensible: add listeners for analytics, reporting
}
