import { /*Command*/ } from 'shared-models/collab-services/commands';

export function handleCollabServicesCommand(cmd: unknown) {
  // Validate, persist, emit event
  // Extensible: add hooks for new types
}
