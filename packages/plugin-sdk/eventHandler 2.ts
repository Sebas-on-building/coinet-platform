import { /*Event*/ } from 'shared-models/plugin-sdk/events';

export function onPluginSdkEvent(event: unknown) {
  // Update projections, trigger downstream
  // Extensible: add listeners for analytics, reporting
}
