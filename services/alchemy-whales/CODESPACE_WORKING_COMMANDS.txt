# ✅ WORKING Commands for Codespace
# Copy and paste these ONE AT A TIME

# Step 1: Navigate to service directory
cd /workspaces/coinet-platform/services/alchemy-whales

# Step 2: Pull latest code
git pull origin feature/ai-data-feeder

# Step 3: Run standalone setup (this handles everything)
chmod +x STANDALONE_SETUP.sh
./STANDALONE_SETUP.sh

# Step 4: Edit .env with your API keys
code .env

# Step 5: Start the service
npm start

# Step 6: Verify (in another terminal)
curl http://localhost:8080/health

# ============================================
# If STANDALONE_SETUP.sh doesn't work, try this:

cd /workspaces/coinet-platform/services/alchemy-whales

# Clean everything
rm -rf node_modules package-lock.json dist

# Install with explicit local flag
npm install --legacy-peer-deps --no-workspaces

# Build
npx tsc

# Create .env
cp .env.example .env

# Start
node dist/index.js

