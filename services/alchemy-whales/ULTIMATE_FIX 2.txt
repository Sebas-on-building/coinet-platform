# 🚨 ULTIMATE FIX - Copy these commands ONE BY ONE into Codespace

# Step 1: Navigate and pull latest
cd /workspaces/coinet-platform/services/alchemy-whales
git pull origin feature/ai-data-feeder

# Step 2: Run the bypass script
chmod +x BYPASS_NPM_SETUP.sh
./BYPASS_NPM_SETUP.sh

# Step 3: If script fails, try manual compilation:
cd /workspaces/coinet-platform/services/alchemy-whales

# Install TypeScript globally
npm install -g typescript

# Compile directly
npx tsc -p tsconfig.json

# Check if dist was created
ls -la dist/

# If dist/index.js exists, start it:
node dist/index.js

# ============================================
# ALTERNATIVE: If npm is completely broken

# Install dependencies one by one (critical ones)
npm install --save alchemy-sdk --no-workspaces
npm install --save express dotenv pino --no-workspaces
npm install --save pg ioredis prom-client --no-workspaces
npm install --save uuid zod bottleneck --no-workspaces
npm install --save axios axios-retry --no-workspaces

# Then compile
npx tsc

# Then run
node dist/index.js

