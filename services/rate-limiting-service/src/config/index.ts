/**
 * =========================================
 * RATE LIMITING CONFIG
 * =========================================
 * Configuration loading for rate limiting service
 */

import { RateLimitConfig } from '../types';

// Export the loadConfigFromEnv function from index.ts
export { loadConfigFromEnv } from '../index';
