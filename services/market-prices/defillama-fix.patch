diff --git a/services/market-prices/src/providers/defillama-rest.ts b/services/market-prices/src/providers/defillama-rest.ts
index existing..new 100644
--- a/services/market-prices/src/providers/defillama-rest.ts
+++ b/services/market-prices/src/providers/defillama-rest.ts
@@ -336,47 +336,17 @@
   async getPools(): Promise<DeFiLlamaYieldPool[]> {
     const response = await this.request<any>('GET', '/yields');
     
-    // Debug: Log the actual response to understand structure
-    logger.debug('getPools response structure', {
-      responseType: typeof response,
-      isArray: Array.isArray(response),
-      isNull: response === null,
-      isUndefined: response === undefined,
-      keys: response && typeof response === 'object' ? Object.keys(response) : [],
-      responsePreview: response ? JSON.stringify(response).substring(0, 200) : 'null/undefined'
-    });
-    
-    // Handle all possible response structures:
-    // 1. Direct array (most common after request() extracts response.data)
-    if (response && Array.isArray(response)) {
-      logger.debug('getPools: Returning direct array', { length: response.length });
-      return response;
-    }
-    
-    // 2. Object with data property containing array
-    if (response && response.data) {
-      if (Array.isArray(response.data)) {
-        logger.debug('getPools: Returning response.data array', { length: response.data.length });
-        return response.data;
-      }
-      // Handle nested structures like { data: { pools: [...] } }
-      if (response.data.pools && Array.isArray(response.data.pools)) {
-        logger.debug('getPools: Returning response.data.pools array', { length: response.data.pools.length });
-        return response.data.pools;
-      }
-    }
-    
-    // 3. Object with array property directly (for different API formats)
-    if (response && typeof response === 'object') {
-      // Check common property names
-      const arrayProps = ['pools', 'yields', 'data', 'results'];
-      for (const prop of arrayProps) {
-        if (response[prop] && Array.isArray(response[prop])) {
-          logger.debug(`getPools: Returning response.${prop} array`, { length: response[prop].length });
-          return response[prop];
-        }
-      }
-      
-      // Last resort: find any array property
-      for (const key in response) {
-        if (Array.isArray(response[key])) {
-          logger.debug(`getPools: Returning response.${key} array (found via iteration)`, { length: response[key].length });
-          return response[key];
-        }
-      }
-    }
-    
-    // Return empty array if response structure is unexpected
-    logger.warn('getPools: Returning empty array - response structure not matched', {
-      responseType: typeof response,
-      response: response
-    });
-    return [];
+    // Simplified: just return response if it's an array, otherwise empty array
+    return Array.isArray(response) ? response : [];
   }

