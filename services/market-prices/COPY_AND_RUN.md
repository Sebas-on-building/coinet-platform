# 📋 Copy & Run Simple Example

## ✅ Step 1: Copy File to Codespace

**LOCAL**: `services/market-prices/quick-example.ts`  
**CODESPACE**: `services/market-prices/quick-example.ts`

1. **LOCAL**: Open `services/market-prices/quick-example.ts`
2. **LOCAL**: Select All (Cmd+A) → Copy (Cmd+C)
3. **CODESPACE**: Create new file `quick-example.ts`
4. **CODESPACE**: Paste (Cmd+V) → Save (Cmd+S)

---

## ✅ Step 2: Run It

```bash
cd /workspaces/coinet-platform/services/market-prices

export CRYPTOPANIC_AUTH_TOKEN=07b3e13fd6db44d4b23fc48ea1cb85a6e739ccb3
npx ts-node quick-example.ts
```

---

## ⚠️ Remember

- **Don't paste TypeScript code in bash terminal**
- **Save code in a `.ts` file first**
- **Then run with `npx ts-node filename.ts`**

---

**Copy the file and run it!** 🚀

