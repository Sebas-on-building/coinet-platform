Fix the 4 failing DeFiLlama tests in divine perfection:

1. Read `src/tests/defillama.test.ts` and identify how the axios mocks structure responses for `/yields` and `/stablecoins` endpoints

2. Update `src/providers/defillama-rest.ts`:
   - Fix `getPools()` to parse the mocked response and return `[{ symbol: 'USDC', apy: 3.5 }]`
   - Fix `getPoolById('pool-id-1')` to return `{ pool: 'pool-id-1', ... }`
   - Fix `getStablecoins()` to parse the mocked response and return `[{ symbol: 'USDT', circulating: { peggedUSD: 80000000000 } }]`
   - Fix `getStablecoin('USDC')` to return `{ symbol: 'USDC', ... }`

3. Ensure response parsing matches exactly what the test mocks provide (check if mocks return direct arrays, wrapped in `data`, or nested properties)

4. Run `npm run test:defi` and verify all 24 tests pass (currently 4 failing, 20 passing)

The issue is that the current implementation returns empty arrays/undefined because it's not parsing the mocked axios response structure correctly. Match the mock structure exactly.

