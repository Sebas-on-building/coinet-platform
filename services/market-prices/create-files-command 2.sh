#!/bin/bash
# Copy this entire command and run it in Codespace
# It will create all new files in one go

cd /workspaces/coinet-platform/services/market-prices && \
mkdir -p src/services src/utils && \
echo "✅ Directories created" && \
echo "📝 Now creating files... (this will take a moment)"
