# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs-20_x", "pnpm"]

[phases.install]
cmds = ["cd services/ai-data-feeder && pnpm install"]

[phases.build]
cmds = ["cd services/ai-data-feeder && pnpm build"]

[start]
cmd = "cd services/ai-data-feeder && node dist/index.js"

